{
  "name": "cors-proxy",
  "private": true,
  "scripts": {
    "deploy:1": "wrangler deploy --config wrangler-1.toml",
    "deploy:2": "wrangler deploy --config wrangler-2.toml",
    "deploy:3": "wrangler deploy --config wrangler-3.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
